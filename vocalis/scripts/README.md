# Helper Scripts

This directory contains convenience PowerShell scripts for local development on Windows.

- `setup.ps1`: Automates environment initialization, installs dependencies across npm workspaces, and builds shared type definitions.
- `dev.ps1`: Launches both backend and frontend development servers concurrently in a single PowerShell window.
